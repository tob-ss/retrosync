apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: preproduction
spec:
  type: NodePort
  selector:
    app: mysql
  ports:
    - name: mysql
      nodePort: 32000
      protocol: TCP
      port: 3306
      targetPort: 3306
