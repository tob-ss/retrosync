apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: preproduction
spec:
  selector:
    app: api-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30020
  type: NodePort