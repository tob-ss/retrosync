apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: preproduction
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: react-app
          image: tobsss/frontend-preprod:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80